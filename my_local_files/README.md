# Local Files Backup

This directory contains standalone snapshots of legacy backend service utilities:
- `api.py`: FastAPI server snapshot with `/simulate`, `/predict`, `/ai/advisory`, TreeSHAP XAI, RSF survival curve, and dynamic prescriptive ROI calculations.
